<h1> <%= @pin.title %> </h1>
<p>
  <%= @pin.description %>
</p>

<%= link_to "back",root_path %>
<%= link_to "Edit", edit_pin_path(@pin) %>
<%= link_to "Delete", pin_path(@pin), method: :delete, data: {confirm: "Are U sure?"} %>
